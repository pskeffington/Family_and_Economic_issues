# Family and Economic Issues

**Status:** JFEI pre-submission cleanup / blinded-package review.  
**Maintainer:** Paul Skeffington, MS, MPH  
**Last documentation refresh:** 2026-05-25

LaTeX manuscript repository for research on family formation, financial preparedness, birth-control education, and completed fertility using the National Longitudinal Survey of Youth 1979 (NLSY79).

## Current manuscript

**Working title:** *Financial Preparedness, Birth-Control Education, and Family Formation in the NLSY79*

This repository is in pre-submission form. It is organized for reproducible drafting, contributor review, citation verification, model-output audit, and automated LaTeX compilation. The manuscript should not be treated as a final journal submission package until the pre-submission checklist below is complete.

## Repository structure

```text
manuscript/
  NLS-79.tex                 # blinded manuscript source
  references.bib             # manuscript bibliography

analysis/
  NLS79_manuscript_analysis_public_git.R

tables/
  CSV and LaTeX audit outputs generated by the analysis script

submission/
  NLS-79_title_declarations.tex # separate title page and declarations; not part of blinded manuscript
  cover-letter, disclosure, and checklist materials; not part of blinded manuscript

.github/workflows/
  build-latex.yml

REPRODUCIBILITY.md
CONTRIBUTING.md
ROADMAP.md
CHANGELOG.md
CITATION.cff
LICENSE
```

## Data files

The analysis script expects public-use NLSY79 extract CSV files in a local data directory. The default directory is:

```text
NLS79 DATA/
```

Expected filenames are:

```text
NLS79-FERTILITY.csv
NLS79_Data_Raw.csv
nls79_emergency_fe_result.csv   # optional precomputed respondent FE result
```

The script retains fallback support for the original local extract names `Capstone_data2.csv` and `default324.csv`. Public-use NLSY79 data should be obtained through the U.S. Bureau of Labor Statistics / NLS Investigator system. Repository users must follow NLSY79 public-use data rules when storing or redistributing extracts.

Raw public-use extract files are intentionally ignored by `.gitignore`. Generated audit files in `tables/` may be committed when they are needed to document manuscript results.

## Reproducing analysis outputs

From the repository root, run:

```bash
Rscript analysis/NLS79_manuscript_analysis_public_git.R
```

With an explicit data directory:

```bash
NLS79_DATA_DIR="NLS79 DATA" Rscript analysis/NLS79_manuscript_analysis_public_git.R
```

The script writes audit files and LaTeX fragments to:

```text
tables/
```

Current generated CSV audit outputs include:

```text
table1_descriptive_summary.csv
table1b_model_specific_samples.csv
table2_main_results.csv
table2b_effect_sizes.csv
table3_sensitivity_diagnostics.csv
variable_crosswalk.csv
```

Current generated LaTeX fragments include:

```text
table1_descriptive_summary.tex
table1b_model_specific_samples.tex
table2_main_results.tex
table3_sensitivity_diagnostics.tex
variable_crosswalk.tex
```

The script also writes respondent-level and panel analytic audit files locally:

```text
analytic_marital_panel.csv
analytic_person_file.csv
```

The current manuscript embeds journal tables directly in `manuscript/NLS-79.tex` for compilation stability. Generated table fragments remain useful for audit, review, and future conversion back to an `\input{}`-based manuscript workflow.

## Automated LaTeX build

The GitHub Actions workflow compiles:

```text
manuscript/NLS-79.tex
```

The compiled PDF is uploaded as a workflow artifact named:

```text
NLS-79-compiled-pdf
```

The workflow runs when manuscript, table, submission-support, figure, or workflow files are pushed to `main`. A `figures/` directory is not currently required, but the workflow is ready for figure assets if they are added later. The workflow can also be started manually from the GitHub Actions tab.

## Submission packaging

The manuscript file under `manuscript/` is intended to remain blinded for peer review. Author-identifying materials should be kept separately under `submission/` and should not be inserted into the blinded manuscript source.

Recommended submission-support files:

```text
submission/NLS-79_title_declarations.tex
submission/cover_letter.md
submission/author_disclosures.md
submission/submission_checklist.md
```

Generated upload bundles or exported Word/PDF files may be created locally under `submission/`, but the source-of-truth materials are the editable manuscript and submission-support files.

## Documentation and review standards

This repository should follow the science-repository pattern in `Best-Practices-Git`: explicit status language, raw-data exclusion, reproducible execution notes, source-verification records, and manuscript-claim traceability.

Current documentation priorities:

- Keep `REPRODUCIBILITY.md` aligned with the active script and local data-directory expectations.
- Keep `CITATION.cff`, `references.bib`, and manuscript citations synchronized.
- Keep generated tables traceable to the analysis script and manuscript text.
- Maintain a clear boundary between blinded manuscript files and author-identifying submission materials.
- Preserve NLSY79 public-use data restrictions by excluding raw extracts from Git.

## Pre-submission checklist

- Confirm the GitHub Actions LaTeX build passes.
- Confirm `manuscript/NLS-79.tex` remains blinded.
- Confirm title-page and cover-letter files are separate from the blinded manuscript.
- Confirm generated table values match the manuscript tables.
- Confirm `references.bib` has verified metadata, DOI links where available, and title capitalization appropriate for the target journal.
- Confirm no raw NLSY79 extract files are committed.
- Confirm `REPRODUCIBILITY.md`, `CITATION.cff`, and `LICENSE` are current.

## Contributor priorities

Current priorities are citation verification, LaTeX compilation stability, table reproducibility, model-output alignment, journal-format cleanup, and transparent documentation of analytic limitations.

## Notes

Build artifacts and raw local data files are intentionally ignored. Commit manuscript source, bibliography files, analysis scripts, audit tables needed to support reported results, reproducibility documentation, submission-support source files, and figure assets if figures are added. Do not commit LaTeX auxiliary files or raw NLSY79 extracts.
